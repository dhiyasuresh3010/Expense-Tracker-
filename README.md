# Expense-Tracker-
A Python-based Expense Tracker application that helps users manage daily expenses efficiently. It supports adding, viewing, editing, deleting, and analyzing expenses with features like category summary, monthly reports, sorting, and CSV export. Data is stored using JSON for persistence and easy retrieval.
